<%= form_with(model: outfit, local: true) do |form| %>
    <% if outfit.errors.any? %>
        <div id="error_explanation">
        <h2><%= pluralize(outfit.errors.count, "error") %> prohibited this outfit from being saved:</h2>
        <ul>
          <% outfit.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
        </div>
      <% end %>
 <div class="cover-new">
  <div class="container mx-auto" style="width: 220px;">
  <form>
    <div class="field">
        <%= form.label :タイトル %>
        <%= form.text_field :title, :placeholder => "タイトルを入力" %>
    </div>

    <div class="field">
        <%= form.label :content %>
        <%= form.text_area :content, :placeholder => "詳細を入力" %>
    </div>

    <div class="field">
      <%= form.label :image %>
      <%= form.file_field :image, autofocus: false %>
      <%= form.hidden_field :image_cache %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
   <% end %>
  </form>
  </div>
 </div>
</div>
